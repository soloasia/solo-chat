export const decimal_point = 2;
export const main_padding = 15;
export const large_padding = 24;
export const activeOpacity = 0.2;
export const isWhitespaceOrEmpty = (params : string) : boolean => (params || '').trim().length === 0;